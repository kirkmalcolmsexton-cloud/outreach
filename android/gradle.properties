org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8
org.gradle.caching=true
org.gradle.configuration-cache=true
org.gradle.configuration-cache.parallel=true
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# App version — bump here for releases (tracked in git; CI uses this file as-is).
# Put real MAPS_API_KEY in android/local.properties or env — keep placeholders only here.
outreach.versionCode=6
outreach.versionName=0.1.5

MAPS_API_KEY=your_maps_key_here
