# Copy to android/keystore.properties (gitignored). Used for local ./gradlew bundleRelease only.
# Paths below are relative to this file's directory (android/).
#
# storeFile=upload-keystore.jks
# storePassword=
# keyAlias=upload
# keyPassword=
